"""Entry point for ``python -m episteme``.

The package currently exposes the library API only. Consumer interfaces
can be added as separate adapters.
"""
if __name__ == "__main__":
    raise SystemExit("No default interface configured. Use the episteme library API directly.")
